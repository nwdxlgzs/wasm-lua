diff --git a/literals.lua b/literals.lua
index d380705..d929c23 100644
--- a/literals.lua
+++ b/literals.lua
@@ -296,7 +296,8 @@ end
 
 
 -- testing decimal point locale
-if os.setlocale("pt_BR") or os.setlocale("ptb") then
+if not rawget(_G, "_WASM") and
+   (os.setlocale("pt_BR") or os.setlocale("ptb")) then
   assert(tonumber("3,4") == 3.4 and tonumber"3.4" == 3.4)
   assert(tonumber("  -.4  ") == -0.4)
   assert(tonumber("  +0x.41  ") == 0X0.41)
